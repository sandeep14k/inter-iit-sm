export default function (item) {
  return {
    RollNo: item.RollNo,
    Department: item.Department,
    Sport: item.Sport,
    Email: item.Email,
    PhoneNo: item.PhoneNo,
    Collage: item.Collage,
    Gender: item.Gender,
    Name: item.Name,
    Photo: item.Photo
  }
}









